int main() {
    int n;
    cin >> n;
    vector<int> k(n);
    for (int &i: k) cin >> i;

    for(int i=0;i<(1<<n);i++){
        for(int j=0;j<n;j++){
            if(i&(1<<j)) {
                // 処理
            }
        }
    }
}
