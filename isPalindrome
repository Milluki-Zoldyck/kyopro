// xをa進数に変換したときに回文かどうかを返す。
bool isPalindrome(ll x, int a){
    string  s;
    while(x){
        s += '0' + x%a;
        x /= a;
    }
    string rs=s;
    reverse(rs.begin(), rs.end())
    return s == rs;
}
